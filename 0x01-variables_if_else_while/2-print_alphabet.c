#include <stdio.h>

/**
 * main - print out alphabet
 *
 * Description: print lower case alphabet
 * using putcharand for loop
 *
 * Return: set to 0
 */
int main(void)
{
	int a, z;

	a = 97;
	z = 122;
	for (a = 97; a <= 122; a++)
	putchar(a);
	return (0);
}
