#include <stdio.h>
#include "main.h"

/**
 * main - prints its name
 * @argc: arguments count
 * @argv: arguments vector
 * Return: 1
 */

int main(int argc, char *argv[])
{
	int a = 0;

	printf("%s\n", argv[a]);
	return (1);
}
