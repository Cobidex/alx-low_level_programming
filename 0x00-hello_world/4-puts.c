#include <stdio.h>

/* program to print out string to stdout */
int main(void)
{
	/* initializing ch as strings */
	char ch = "\"Programming is like building a multilingual puzzle";

	/* write out to stdout */
	puts(ch);

	return (0);
}
