#include "main.h"

/**
 * _abs - print absolute
 * @int: number
 *
 * Return: return absolute value
 */
int _abs(int)
{
	if (int < 0)
		return (-int);
	else if (int > 0)
		return (int);
	else
		return (int);
}
