#include "main.h"

/**
 * _abs - print absolute
 * @c: number
 *
 * Return: return absolute value
 */
int _abs(int c)
{
	if (c < 0)
		return (-c);
	else if (c > 0)
		return (c);
	else
		return (c);
}
