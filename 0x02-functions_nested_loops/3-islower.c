#include "main.h"
/**
 * _islower - checks if lower case
 * @c: alphabet to be entered
 *
 * Return: 1 if lowercase, 0 if not
 */
int _islower(int c)
{
	int c;

	if (c < 123 & c > 96)
		return (1)
	else
		return (0)
}
